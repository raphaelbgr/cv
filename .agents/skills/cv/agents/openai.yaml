interface:
  display_name: "Cv"
  short_description: "Repo-specific patterns and workflows for cv"
  default_prompt: "Use the cv repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true